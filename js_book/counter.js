let counter = 1;
while (counter <= 10) { // change the conditional expression to log more numbers
  console.log(counter);
  counter += 1; // modify counter in some way that will eventually make the condition false
}