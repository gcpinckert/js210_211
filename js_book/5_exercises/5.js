function scream(words) {
  return words + '!!!!';
}

scream('Yipeee');

// The above program does not output anything because console.log is never called
// It returns a value, but nothing is done with this